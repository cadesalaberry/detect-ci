{
  "name": "detect-ci",
  "version": "0.0.1",
  "scripts": {
    "test": "yarn detect-ci",
    "postinstall": "yarn detect-ci",
    "detect-ci": "UNAVAILABLE='' echo \"CI:$CI\" && $UNAVAILABLE || echo 'NOT IN CI' && false"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
