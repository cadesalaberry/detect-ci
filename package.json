{
  "name": "detect-ci",
  "version": "0.0.1",
  "scripts": {
    "test": "yarn detect-ci",
    "postinstall": "yarn detect-ci",
    "detect-ci": "echo \"CI:$CI\" && $CI || (echo 'NOT IN CI' && false)"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
